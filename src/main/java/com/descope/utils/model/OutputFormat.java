package com.descope.utils.model;

/** Enumeration of supported output formats for CLI commands. */
public enum OutputFormat {
  /** Human-readable text output format. */
  TEXT,

  /** JSON output format for programmatic consumption. */
  JSON
}
